part of '../borrowed_tools.dart';

class BorrowedToolsPage extends StatelessWidget {
  const BorrowedToolsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Placeholder(),
    );
  }
}
